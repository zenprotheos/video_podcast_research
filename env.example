# Required for DEAPI transcription fallback and podcast transcription
DEAPI_API_KEY=your_deapi_api_key_here

# Required for YouTube search functionality
YOUTUBE_DATA_API_KEY=your_youtube_api_key_here

# Required for AI video filtering
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: override base URL if needed
# DEAPI_BASE_URL=https://api.deapi.ai

# Optional: model hint (depends on DEAPI)
# DEAPI_MODEL=WhisperLargeV3

# Optional: default AI model for video filtering (defaults to openai/gpt-4o-mini)
# OPENROUTER_DEFAULT_MODEL=openai/gpt-4o-mini

# Required for Proxy-based transcript extraction (Bulk Transcribe Proxy page)
# Path to WebShare residential proxy credentials file
# Format: host:port:username:password (one per line)
WEBSHARE_PROXY_FILE=my_assets/Webshare residential proxies.txt